# 현재 문제점 정리

---

## [수정 완료] 구조적 문제 (이번 세션에서 해결)

| 문제 | 파일 | 조치 |
|------|------|------|
| 모든 피처 OFF 프로세스가 supervisor에 미등록 → `set` 명령 NotFound | `main.cpp` | `registerProfile()`로 항상 등록 |
| disabled feature가 argv에 안 넘어감 → 런타임 ON 시 앱이 SHM 미연결 | `ProcessSupervisor.cpp` | 모든 feature ID 무조건 전달 |
| `setFeatureFlag`에서 꺼진 프로세스 자동 기동 없음 | `ProcessSupervisorFeature.cpp` | `isAliveLocked` 체크 후 `launchLocked` |
| 모든 피처 OFF 시 앱이 루프를 계속 도는 문제 | `SdvAppFrame.cpp` | `any_enabled` 체크 후 자율 종료 |
| 자율 종료 후 Watchdog이 재시작 반복 | `Watchdog.cpp` | `hasAnyEnabledFeature()` 체크 추가 |
| BUG-1: 재기동 후 피처 OFF 시 프로세스 미종료 (SHM unlink 타이밍) | `ProcessSupervisor.cpp` | SHM을 `registerProfile`에서 생성, 프로세스 수명과 분리 |
| DEBT-6: profile flag 3중 저장 (`all_profiles_`, `registry_.profile`, SHM) | `ProcessRecord.h`, `Watchdog.cpp` | `ProcessRecord`에서 profile 제거, Watchdog이 `all_profiles_` 직접 참조 |
| DEBT-3: 소멸자 + Watchdog `waitpid` 경쟁 | `Watchdog.h/cpp`, `ProcessSupervisor.cpp` | `Watchdog::stop()` 추가, 소멸자에서 먼저 호출 |
| DEBT-2: `setKilled()` 미연결 | `ProcessSupervisorKill.cpp`, `ProcessSupervisor.cpp` | `hardKill`에서 is_killed 설정 후 1초 대기, 재기동 시 is_killed 초기화 |

---

## [미해결] 설계 부채

### DEBT-1: `action_on_failure` 미사용
- **위치**: `FeatureProfile.h:8`, `ManifestLoader.cpp`, `Watchdog.cpp`
- **내용**: manifest에서 파싱하고 구조체에 저장되지만, Watchdog에서 실제 동작에 반영하지 않음. 항상 `Disabled`로 고정 처리.
- **영향**: 낮음 (현재 action이 하나라 문제없음)

### DEBT-4: `ShmManager::openForRead` 이름 불일치
- **위치**: `ShmManager.cpp:49`
- **내용**: `O_RDWR`로 열고 있어서 앱에서도 SHM에 쓸 수 있는 상태. 이름이 Read 전용임을 암시해 오해 유발.
- **영향**: 낮음

### DEBT-5: `ProcessRecord` 기본 state
- **위치**: `ProcessRecord.h`
- **내용**: 이전엔 기본값이 `Running`이었고 현재는 `Disabled`로 수정됨. 의미는 맞지만 registry에 처음 등록될 때 명시적으로 세팅되므로 기본값이 실제로 쓰이는 경우는 없음.
- **영향**: 낮음

---

## 우선순위 요약

| 우선순위 | 항목 | 작업 |
|----------|------|------|
| 🟢 여유 | DEBT-1, 4, 5 | 이름 정리 및 action_on_failure 구현 |
